#include <stdio.h>

void (char a[100],int b) {
	char nama[100];
	int c,d;
	FILE *fp;
	fp = fopen ("stock","w+");
	fscanf (fp,"%s,%d,%d",nama,&c,&d);
	if (a == nama) {
		
	}
}

int main () {
	
}
